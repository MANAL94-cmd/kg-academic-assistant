# This repo has two apps:
#   • Primary: React frontend (frontend/) + Flask backend (backend/requirements.txt)
#   • Legacy:  Streamlit app (legacy/requirements.txt)
#
# This root file exists so Streamlit Community Cloud (which installs from the
# repo-root requirements.txt) can still deploy the legacy app pointed at
# legacy/streamlit_app.py. It simply re-uses the legacy dependency list.
-r legacy/requirements.txt
